= f.input :name
= f.input :avatar, as: :file
- if f.object.avatar.url.present?
  = image_tag f.object.avatar, class: 'u-centered profileImage img-circle'
= f.association :categories, as: :check_boxes, collection: Category.where(people: true)
